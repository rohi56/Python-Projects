#built-in functions and lists
runs = [100, 85, 51, 22, 68]
print('number of match in series:', len(runs))
print('number of runs scored in series:', sum(runs))
print('max runs scored in series:', max(runs))
print('min runs scored in series:', min(runs))
print('average runs scored in series:', sum(runs)/len(runs))
